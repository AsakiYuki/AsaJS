export enum AnimType {
    ALPHA = "alpha",
    CLIP = "clip",
    COLOR = "color",
    FLIP_BOOK = "flip_book",
    OFFSET = "offset",
    SIZE = "size",
    UV = "uv",
    WAIT = "wait",
    ASEPRITE_FLIP_BOOK = "aseprite_flip_book",
}